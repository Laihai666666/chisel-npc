// Generated by CIRCT firtool-1.62.0
// Standard header to adapt well known macros for prints and assertions.

// Users can define 'PRINTF_COND' to add an extra gate to prints.
`ifndef PRINTF_COND_
  `ifdef PRINTF_COND
    `define PRINTF_COND_ (`PRINTF_COND)
  `else  // PRINTF_COND
    `define PRINTF_COND_ 1
  `endif // PRINTF_COND
`endif // not def PRINTF_COND_

module PerformanceCounter(
  input clock,
        reset,
        io_ifu_fetch,
        io_lsu_load,
        io_exu_exec,
        io_idu_alu,
        io_idu_mem,
        io_idu_csr,
        io_finish
);

  reg [63:0] ifu_counter;
  reg [63:0] lsu_counter;
  reg [63:0] exu_counter;
  reg [63:0] idu_alu_counter;
  reg [63:0] idu_mem_counter;
  reg [63:0] idu_csr_counter;
  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if ((`PRINTF_COND_) & io_finish & ~reset) begin
        $fwrite(32'h80000002, "Performance Counters (Final):\n");
        $fwrite(32'h80000002, "  IFU Fetches: %d\n", ifu_counter);
        $fwrite(32'h80000002, "  LSU Loads: %d\n", lsu_counter);
        $fwrite(32'h80000002, "  EXU Executes: %d\n", exu_counter);
        $fwrite(32'h80000002, "  IDU ALU Ops: %d\n", idu_alu_counter);
        $fwrite(32'h80000002, "  IDU Mem Ops: %d\n", idu_mem_counter);
        $fwrite(32'h80000002, "  IDU CSR Ops: %d\n", idu_csr_counter);
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
  always @(posedge clock) begin
    if (reset) begin
      ifu_counter <= 64'h0;
      lsu_counter <= 64'h0;
      exu_counter <= 64'h0;
      idu_alu_counter <= 64'h0;
      idu_mem_counter <= 64'h0;
      idu_csr_counter <= 64'h0;
    end
    else begin
      if (io_ifu_fetch)
        ifu_counter <= ifu_counter + 64'h1;
      if (io_lsu_load)
        lsu_counter <= lsu_counter + 64'h1;
      if (io_exu_exec)
        exu_counter <= exu_counter + 64'h1;
      if (io_idu_alu)
        idu_alu_counter <= idu_alu_counter + 64'h1;
      if (io_idu_mem)
        idu_mem_counter <= idu_mem_counter + 64'h1;
      if (io_idu_csr)
        idu_csr_counter <= idu_csr_counter + 64'h1;
    end
  end // always @(posedge)
endmodule

